# Copy this file to .env and fill in real values. .env is gitignored.
CI_ENV=development

# Full base URL of this install, trailing slash required.
BASE_URL=http://localhost/simmed/

DB_HOSTNAME=localhost
DB_USERNAME=root
DB_PASSWORD=
DB_DATABASE=simmed

# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=
